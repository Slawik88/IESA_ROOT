# NOT IMPLEMENTED — Предвестник V2

> Конкретные доработки уже начатых механик. "Делаем пункт N" → берём отсюда, фиксим, `node --check` / `py_compile`, commit+push.
> Сверено с живым кодом 2026-06-10.

---

*(пусто — все пункты выполнены. Идеи на будущее — в FUTURE_IDEAS.md)*




## БЛОК 15: ГЛОБАЛЬНЫЙ ПАТЧ QOL, ОПТИМИЗАЦИЯ И СИСТЕМА SMART CHECKOUT

> ✅ **ВСЕ 6 ШАГОВ ВЫПОЛНЕНЫ (2026-06-24).**
> - Ш1 история кошелька «Было→Изменение→Стало (причина)» — `wallet.py` + app.06.js.
> - Ш2 аукцион: фильтры(редкость/тип)+сорт+поиск+бейдж редкости — `auction.py` + app.04/06.js.
> - Ш3 БП: заморозка сезона ❄️ (bp_settings, блок XP/наград + плашка) + фикс каскада удаления.
> - Ш4 Крипто-Биржа: `services/crypto_exchange.py` (цены=функция времени, свечи), портфель
>   `crypto_holdings`, `/exchange/crypto[/trade]`, UI со свечами+ползунком % (app.06.js).
> - Ш5 косметика: фикс «пустой ник» (@supports-фолбэк), перф вкладки (анимации свотчей off),
>   глитч на clip-path+text-shadow.
> - Ш6 Smart Checkout: `services/smart_checkout.py` + `buy_item(cover_with_zarniki)` (атомарно,
>   ceil) + `/shop/checkout-quote` + модалки A/B.
> Проверено: py_compile, node --check, import FastAPI.main, render-скриншоты, юнит-тесты.

Ты — Lead Fullstack Developer и UX/UI Designer. Твоя задача — провести масштабный рефакторинг текущих модулей, оптимизировать фронтенд и внедрить систему умной покупки (Smart Checkout). Работы много, разделяй её по шагам.

### ШАГ 1: ЛОГИРОВАНИЕ КОШЕЛЬКА (WALLET HISTORY)
Текущая история транзакций неинформативна.
- **Backend:** В таблицу логов транзакций добавь поля, чтобы фиксировать триггер изменения.
- **Frontend/UI:** История должна отображаться в формате: `[Было] -> [Изменение] -> [Стало]`. 
  *Пример визуализации:* `150 🪙  |  +50 🪙  |  Итог: 200 🪙  (Продажа предмета)`.

### ШАГ 2: РЕФАКТОРИНГ АУКЦИОНА (UX/UI)
Сейчас аукцион — это свалка. Игрокам тяжело ориентироваться.
- Внедри **Фильтры**: по редкости (Common, Rare, Epic и т.д.), по типу предмета (Сферы, Расходники, Питомцы).
- Внедри **Сортировку**: "Сначала дешевые", "Сначала дорогие", "Новые лоты".
- Внедри **Поиск** по названию лота.
- Оформи карточки лотов в аккуратную сетку (Grid) с четким разделением информации: Продавец, Предмет, Цена, Время до конца.

### ШАГ 3: УПРАВЛЕНИЕ БОЕВЫМ ПРОПУСКОМ И ЗАМОРОЗКА
- **Багфикс:** Почини кнопку "Удалить" в админке БП (Шаг 1 - Сезоны). Убедись, что каскадное удаление (CASCADE) работает корректно и не оставляет сиротских записей.
- **Новая фича "Заморозка БП":** 1. Добавь в сущность сезона статус `is_frozen` (boolean). 
  2. Если БП заморожен, Middleware должен блокировать начисление очков (EXP) и выдачу наград всем юзерам.
  3. **Frontend:** Если сезон заморожен, на странице БП у игроков должна висеть красивая плашка ❄️ `Сезон временно заморожен` и прогресс-бар должен быть серым/неактивным.

### ШАГ 4: ВНЕДРЕНИЕ КРИПТО-БИРЖИ НА КЛИЕНТ
- На сайте до сих пор отсутствует интерфейс Биржи. Сверстай вкладку "Биржа" согласно ранее прописанной архитектуре (графики монет, кнопки Купить/Продать, портфель юзера).

### ШАГ 5: ОПТИМИЗАЦИЯ "ВНЕШНЕГО ВИДА" И РЕМОНТ КОСМЕТИКИ
Вкладка сильно лагает и греет телефоны. Это критическая проблема производительности (FPS drops).
- **Оптимизация:** Пересмотри CSS-анимации. Замени тяжелые фильтры (`filter: drop-shadow`, `box-shadow`) и бесконечные перерисовки компонентов на аппаратное ускорение (`transform`, `opacity`, `will-change`). Убери утечки памяти в React/Vue компонентах.
- **Багфикс:** Вместо ника юзера сейчас рендерится пустой прямоугольник. Исправь привязку данных, чтобы косметика применялась именно к тексту `user.nickname`.
- **Редизайн "Глитча":** Текущий эффект глитча на нике выглядит грязно. Перепиши CSS-анимацию Глитча. Используй аккуратный `clip-path` и `text-shadow` (красный/голубой сдвиг), чтобы это выглядело как стильный киберпанк, а не как битый пиксель.

### ШАГ 6: СИСТЕМА УМНОЙ КОНВЕРТАЦИИ "MISSING RESOURCES" В ЗАРНИКИ (SMART CHECKOUT)
Всякий раз, когда юзер пытается купить предмет/улучшение, но у него не хватает базовых валют, мы больше не выдаем глухую ошибку "Недостаточно средств". Скрипт должен перехватывать этот момент:

1. **Расчет дефицита:** Вычислить, сколько конкретно единиц каждого ресурса не хватает.
2. **Конвертация:** Перевести этот дефицит в Зарники по фиксированному курсу. Создай конфигурационный словарь `ZARNIKI_EXCHANGE_RATES` (например: 1 Зарник = 1000 Моры, 1 Зарник = 1 Алмаз).
3. **Проверка баланса:** Проверить, достаточно ли у юзера Зарников, чтобы покрыть этот вычисленный дефицит.

**FRONTEND И UX (ИНТЕРФЕЙСНЫЕ РЕАКЦИИ)**
В зависимости от наличия Зарников на балансе, UI выдает один из двух вариантов модального окна:
* **Сценарий А (У юзера ЕСТЬ нужные Зарники):**
    Текст: "Вам не хватает {X} [Ресурс]. Покрыть недостаток Зарниками?"
    Кнопка покупки меняется на смешанную: `[Купить за {Имеющиеся ресурсы} + {Y} ✨ Зарников]`.
* **Сценарий Б (У юзера НЕТ нужных Зарников):**
    Текст: "Вам не хватает {X} [Ресурс] или {Y} ✨ Зарников для быстрой покупки."
    Кнопка меняется на призыв: `[Купить Зарники ✨]`. При нажатии редиректит во вкладку магазина доната.

**BACKEND И БЕЗОПАСНОСТЬ (КРИТИЧЕСКИЕ ПРАВИЛА)**
- **Округление в пользу казино:** Используй `math.ceil` / `Math.ceil`. Дробных Зарников при списании быть не должно! Если не хватает 1 Моры (а курс 1000 к 1), юзер платит 1 Зарник.
- **Атомарность БД:** Списание ресурсов и Зарников должно происходить в одной SQL-транзакции. Либо списывается всё + выдается товар, либо полный Rollback.
- **Единый хелпер:** Напиши универсальную функцию `CalculateMissingResourcesCost`, которую можно будет оборачивать вокруг любой покупки.

ТВОЙ АУТПУТ:
Предоставь план выполнения (сначала БД/Бэкенд, затем Оптимизация Фронта, затем фичи) и приступай к написанию кода шаг за шагом.