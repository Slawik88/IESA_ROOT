# SESSION HANDOFF — Предвестник V2 (быстрый ввод в курс дела)

> Покажи этот файл боту в начале новой сессии. Здесь: что сделано, КАК ДЕПЛОИТСЯ
> (важно!), почему «код пропадает на сайте», токено-экономия и план рефакторинга.
> База — `BASE_PROMPT.md`/`CLAUDE.md`. Доделки — `NOT_IMPLEMENTED.md`. Идеи —
> `FUTURE_IDEAS.md`. План БП — `BP_V2_PLAN.md`.

## ⚙️ АДМИН-ДОРАБОТКИ — 2 БЛОКА (2026-06-22)
Большой список правок дев-консоли/игры разбит на 2 блока.

### БЛОК 1 — ✅ ГОТОВО (Досье · выдача · аукцион-анонс · float)
- **Досье игрока** (`dev_console.py` `/user`,`/chats` + app.js): чаты — только реальные
  группы (chat_tg_id<0, **без ЛС**); основной чат + его админка размечены (🏠/🛡) и идут
  рядом одной **выделенной группой** (CSS `.dev-chat-grp`, helper `_classify_chat_links`);
  селектор чатов тоже без ЛС и с метками ролей. **VIP-строка**: осталось дней + на сколько
  в целом (span) + стаж (`days_left`/`span_days` в SQL).
- **🎁 Выдать предмет**: кнопка «📋 Каталог предметов» — модалка с поиском по
  названию/id/категории (полный `ITEMS_REGISTRY`), клик = подставить item_id.
- **👑 VIP — управление**: `−N` дней (`/adjust-vip-days`), продлить (`/give-vip`),
  заменить тариф (`/set-vip`), отозвать (`/revoke-vip`), 🔍 статус, пресеты дней.
  Деструктив — через модалку `OM` (не `confirm()` — его в коде нет).
- **⚖️ Аукцион-анонс**: при создании лота (веб `routers/auction.py` + бот
  `handlers/auction.py`) — сообщение во все **основные чаты** (`get_announce_chats` в
  routing-репо, без админок/ЛС) с описанием лота + кнопка «🔨 Открыть аукцион»
  (`t.me/<BOT_USERNAME>?startapp=auction_<id>`). Builder `build_lot_announcement` в services
  (без bot-импортов). Доставка тихая (не ломает создание лота).
- **💯 Float**: `fmtF` (разделители + до 2 знаков) в app.js; убраны `Math.floor/round`
  в валютном баре, модалке валют, досье, обзоре экономики, ценах реликвий. Колонки
  балансов уже `FLOAT8`, `add_balance` принимает float — бэкенд float держит.

### БЛОК 2 — ✅ ГОТОВО: БП · Рассылка · Theme Lab
- **🎫 БП**: удалить сезон/импорт JSON — бэкенд корректен (DELETE…RETURNING — рабочий
  паттерн, 13 репо его юзают); прежнее «не работает» = несвежая статика на проде,
  не код. Импорт устойчив к float (int-каст). **UI реорганизован**: карточки помечены
  как «Шаг 1 Сезоны → Шаг 2 Таблица → Шаг 3 Редактор наград → Шаг 4 XP за действия» +
  ориентирующая подсказка. **Подсветка «сочности»** ячеек таблицы (`_bpCellScore` →
  классы `.bp-j1..j4`, ярче = ценнее). **«Раскидать пул»** — новый эндпоинт
  `/bp/rewards/distribute` (flat/linear/progressive, сумма точно сходится) + UI.
  Кнопка игрока **«За что XP» переделана** (`.xpg-btn` золотой pill + модалка с
  XP-барами, сортировка по убыванию, лимиты/день).
- **📢 Рассылка**: растягиваемое поле + **живое превью** (HTML) + **фильтры аудитории**
  (основные / админки / основные+админки / ЛС+админки / ЛС / все) через
  `routing_repo.get_broadcast_targets`; счётчик получателей (`/broadcast/audience-counts`).
- **🎨 Theme Lab**: **полный список тегов** — `get_template_variables` теперь отдаёт
  универсальные теги ВСЕГДА (раньше только встречавшиеся в тексте → `{титул}` и пр. были
  скрыты) + тема-специфичные; **модалка-справка** «что выводит каждый тег»
  (`get_template_var_help` → `var_help` в API, кнопка `devTLVarHelp`).
- Проверено: `py_compile` (3 файла) + `node --check` — OK; дублей JS-функций нет.

## 🧭 ОЧЕРЁДНОСТЬ ЗАДАЧ (делать ПО ПОРЯДКУ)
1. **Глобальный стрик — ✅ ГОТОВО.** 1 стрик на ВСЕ чаты через sentinel `chat_id=0` в
   `daily_login` (без новой таблицы). Единая таймзона `STREAK_TIMEZONE_OFFSET=3` (МСК).
   Репо-обёртки `get/upsert/update_global_streak` (`infrastructure/repositories/streak.py`);
   middleware и бот-хендлер пишут/читают глобальную строку; первое сообщение дня в любом
   чате атомарно забирает день → одна награда/день. Миграция в `database.py` засевает
   `chat_id=0 = MAX(streak)` по юзеру (идемпотентно). Сайт/профиль читают `MAX(streak)` (=
   глобальная строка). **Вкладка стрика полностью переделана** (`loadStreak` + CSS `.st-*`):
   hero-блок с прогрессом блока, 7-дневный трек наград, хитмап активности 60 дн.
2. **Новые слоты косметики — ✅ ГОТОВО.** 3 новых слота (по согласованию с юзером, все 3 +
   все 4 источника + по 6 предметов): `avatar_halo` (гало, css `halo-*`), `profile_bg`
   (фон карточки, `pbg-*`), `card_fx` (частицы поверх карточки, `cfx-*`). Реестр в
   `core/cosmetics.py` (`COSMETIC_SLOTS` + 18 записей: shop/vip/bp/reward, common→mythic).
   Сервис `services/cosmetics.py` слото-агностичен (каталог/покупка/экипировка/auto-grants —
   без правок). Веб: профиль применяет классы (`.hero`/`.ava`/overlay `.card-fx`),
   конструктор «Внешний вид» рендерит новые слоты (`_LOOKS_SLOTS` + превью). CSS — полный
   набор `halo-*`/`pbg-*`/`cfx-*` с анимациями. ⚠️ reward-предметы (halo_void/pbg_legend/
   cfx_void_storm) ждут вызова `grant_cosmetic` из ачивок (как title_abysswalker) — пока
   недоступны до выдачи. Бот не трогаем (визуал — web-only по дизайну).
3. **Админ-БЛОК 2 — ✅ ГОТОВО** — БП / Рассылка / Theme Lab (детали в разделе выше).
4. **Рефакторинг** монолитов (`app.js` ~6100 строк и др., см. раздел 5).

> Сделано в этой сессии помимо БЛОКА 1: косметика — приветствия для **не-VIP** (превью
> доступно всем, покупка/выбор — с VIP, баннер «Перейти к VIP» в модалке «Внешний вид»).

## 0. ОКРУЖЕНИЕ / ОГРАНИЧЕНИЯ (важно для проверок)
- Базовая проверка кода = `py_compile` + `node --check`. ДОПОЛНИТЕЛЬНО можно поставить
  локально `fastapi uvicorn pydantic loguru pyflakes` (`pip install`) → тогда доступны
  **import-тест** (`import FastAPI.main`), перечисление роутов через `app.openapi()` и
  статанализ `pyflakes` (ловит undefined-name в телах). Так верифицирован сплит dev_console.
- **БД DigitalOcean** доступна с машины разработчика ТОЛЬКО если его IP добавлен в
  DO → база → Settings → **Trusted Sources** (иначе таймаут). `DATABASE_URL` уже в
  `predvestnik_v2/.env`; `asyncpg` ставится локально. Так верифицирован сплит database.py
  (read-only сверка схемы). ⚠️ ПРОД — только чтение (SELECT/схема), никаких ALTER/DROP/DDL.
- Деплой: пуш в GitHub (`Slawik88/IESA_ROOT`, папка `predvestnik_v2/`) → DigitalOcean.
- Коммитит/пушит ПОЛЬЗОВАТЕЛЬ сам (бот код не пушит, если явно не попросят).

## 1. ⚠️ ГЛАВНОЕ: ПОЧЕМУ «КОД ПРОПАДАЕТ НА САЙТЕ» (диагноз 2026-06-22)
Код фич **физически есть в репозитории и подключён** (проверено grep'ом):
кнопка экспедиции (`expLauncherHtml` объявлена в app.js и вызывается в `renderZoo`),
конструктор косметики (`openLooksModal` + кнопка «🎨 Внешний вид» на профиле),
конструктор XP БП, превью наград, таймер сезона, обменник и т.д. — всё на месте.

**Корневая причина «не видно на сайте» — НЕ код, а доставка:**
`FastAPI/main.py` отдаёт статику ИЗ ПАМЯТИ:
- `_APP_JS = _read_static("app.js")` (main.py:182) — читается **один раз при старте процесса**.
- роут `/static/app.js` (main.py:195) возвращает эту переменную.
- `_ASSET_VER` (main.py:168) — тоже фиксируется один раз при старте (по mtime файлов).

➡️ **Пока процесс на DigitalOcean не перезапустится (новый деплой/рестарт), сайт
отдаёт СТАРЫЙ `app.js`/CSS, прочитанный при прошлом старте — даже если новый код
уже в GitHub.** Отсюда ощущение «половина работы испарилась».

### Что должен проверить пользователь (у бота нет доступа к проду/DO/GitHub):
1. Открыть напрямую `https://iesaroot-app-8kuyb.ondigitalocean.app/predvestnik/static/app.js`
   и Ctrl+F поискать `openLooksModal` или `expLauncherHtml`.
   - **НАЙДЕНО** → код задеплоен, проблема в кеше браузера/Telegram WebApp → жёсткий
     refresh; проверить, меняется ли `?v=...` в исходнике страницы.
   - **НЕ НАЙДЕНО** → процесс крутит старый код → **DigitalOcean не передеплоил/не
     перезапустился** после пуша.
2. В DigitalOcean → Activity/Deployments: триггерится ли деплой на пуш? Билд успешен?
3. Совпадает ли ветка, которую слушает DO, с веткой, в которую пушит пользователь (`master`)?
4. Сравнить `?v={{ASSET_VER}}` в исходнике страницы до/после нового деплоя — меняется ли число.

### Рекомендованные фиксы (когда дойдут руки):
- **Основной (операционный):** убедиться, что DO **autodeploy + рестарт** на пуш в нужную
  ветку включён и билд зелёный. Без рестарта контейнера статика из памяти не обновится.
- **Код-усиление (опц.):** заменить `_ASSET_VER` с mtime на **хеш содержимого**
  (md5 app.js+app.css) — устойчивее к причудам mtime при чекауте. (Поможет ТОЛЬКО при
  рестарте процесса — без рестарта всё равно отдаётся старое из памяти.)

## 2. ЧТО СДЕЛАНО (в коде, закоммичено) — БП v2
Полностью по `BP_V2_PLAN.md`:
- **A — анти-абуз:** дневные потолки XP на действие (`bp_xp_daily`, `BATTLE_PASS_XP_DAILY_CAPS`);
  закрыт вош-трейд через `auction_sales` (36 XP/день).
- **B — конструктор весов:** БД-оверрайды `bp_xp_weight_overrides`; `add_xp` читает
  `_effective_xp_config` (БД > constants); `enabled=false`→0 XP; кастом-действия.
- **dev-UI:** карточка «⚙️ Конструктор XP БП» в дев-консоли (вес/потолок/вкл-выкл/кастом).
- **Страница игрока:** C1 справка «за что XP» (кнопка ℹ️), C2 сочный левел-ап+shimmer-бар+
  «осталось Y XP», C3 превью след. награды, C4 таймер сезона+прогноз, C5 💎-открыть
  уровень (`4+2×(L−2)`, floor = алмазы_уровня+5, анти-цикл, только +1), C6 «Забрать всё»,
  C7 weekend XP boost (флаг в конструкторе).
- Единая точка `add_xp` (зовётся из `increment_metric`) → паритет бот↔сайт.
- Новые таблицы: `bp_xp_weight_overrides`, `bp_xp_daily`, `bp_settings`.

## 3. ЧТО СДЕЛАНО (в коде, закоммичено) — Косметика (конструктор внешнего вида)
- `core/cosmetics.py` — реестр (12 предметов, слоты: ореол имени / рамка аватара / титул),
  гибкая цена: `price` = список вариантов оплаты (OR), внутри — комбинация валют (AND),
  валюты ✨🪙💎🌑. `vip_required`, `source` (shop/vip/bp/reward).
- Таблицы `user_cosmetics` (владение), `user_cosmetic_loadout` (надетый сет).
- `services/cosmetics.py` — каталог, покупка (атомарно, мульти/альт-валюта), экипировка,
  `sync_auto_grants` (vip→при VIP; bp→при платном треке и `bp_level`), `get_active_cosmetics`.
- `FastAPI/routers/cosmetics.py` — `GET /cosmetics/`, `POST buy/equip/unequip`; в main.py.
- Веб-UI: кнопка «🎨 Внешний вид» на профиле → модалка-конструктор с живым превью,
  слот-пикеры, покупка/надеть/снять. Профиль применяет ореол (`.pname`), рамку (`.ava`),
  титул (`.ptitle`); `/profile/me` отдаёт `cosmetics`. CSS `glow-*`/`frame-*`.
- Бот: титул в `profile_render.py` (стандартный рендер) + переменная `{титул}` в ctx.
- **Приветственные анимации (часть A) — ✅ 2026-06-22.** 5 режимов прелоадера
  (scanner/neon/ripple/glitch/stardust); VIP выбирает в «🎨 Внешний вид» → секция
  «🎬 Приветствие» с живым превью, не-VIP — дефолт. Реестр `WELCOME_ANIMATIONS`
  (`core/cosmetics.py`), хранение в `user_cosmetic_loadout` slot=`welcome` (без DDL),
  роут `POST /cosmetics/welcome`, режим = класс `plm-<id>` на `#preloader`.
  Полировка покупки: подсветка нехватки валюты + явный VIP-замок на карточках.

## 4. ТОКЕНО-ЭКОНОМИЯ (почему дорого)
1. **Длинный непрерывный диалог** — главный расход (вся история перечитывается каждый ход).
   Дешевле всего: **новый чат под каждую крупную задачу** + `/compact`.
2. **Монолитные файлы** — приходится grep+читать куски:
   - `FastAPI/static/app.*.js` — БЫЛ один ~6100-строчный `app.js`, теперь **9 частей**
     `app.01.js…app.09.js` (классик-скрипт; main.py склеивает в один — см. §5). Правь нужную часть.
   - `FastAPI/routers/dev_console/` — пакет под-роутеров (`_common`+7 доменов), бывший
     монолит 1436 строк; править нужный домен (см. §5)
   - `services/profile_render.py` ~611 (словарь шаблонов вынесен в `core/profile_templates.py`)
   - `bot/core/database.py` ~1065 — DDL разнесён на 6 доменных под-функций, `init_db` =
     оркестратор (см. §5)

## 5. ПЛАН РЕФАКТОРИНГА ИЕРАРХИИ (поддерживаемость + экономия; делать в СВЕЖЕЙ сессии)
- ✅ **СДЕЛАНО (иначе, чем планировалось):** `app.js` (6129 строк) разбит на **9 файлов**
  `static/app.01.js … app.09.js` (по 357–912 строк). ⚠️ ВАЖНО: подключать отдельными
  `<script>` НЕЛЬЗЯ — top-level `let/const` классического скрипта живут в ОДНОЙ лексической
  области, отдельные теги сломали бы cross-file ссылки (`fmtF`, `_looksData` и т.п.). Поэтому
  `main.py` СКЛЕИВАЕТ части (`_APP_JS_PARTS`, `"".join(...)`) и отдаёт ОДНИМ скриптом на
  `/static/app.js` — index.html не менялся. Гарантия идентичности: склейка == исходник
  байт-в-байт (проверено) + `node --check` собранного. **Правки теперь — в нужный
  `app.0N.js`; редактировать через grep+offset.** ASSET_VER считает max mtime по всем частям.
- ✅ **СДЕЛАНО (2026-06-23, с runtime-проверкой):** `dev_console.py` (1436 строк) → пакет
  `FastAPI/routers/dev_console/`: `_common.py` (хелперы+гейт), `player/vip/system/bp/broadcast/
  sql/themes.py` (под-роутеры по доменам), `__init__.py` склеивает их в `router` с prefix
  `/admin/dev` (main.py не менялся — импортит `dev_console.router` как раньше). Сплит
  механический (AST-вырезка эндпоинтов/моделей/констант + минимальные импорты по факту
  использования). **Верификация:** `pyflakes` 0 undefined-name (нет потерянных импортов) +
  0 unused; `app.openapi()` — 41 dev-роут (method+path) ИДЕНТИЧНЫ исходнику; `import
  FastAPI.main` целиком собирается. NB: в новой FastAPI `include_router` кладёт прокси
  `_IncludedRouter` в `.routes` — роуты считать через `app.openapi()`, не по `router.routes`.
- ✅ **СДЕЛАНО:** `profile_render.py` → словарь `_DEFAULT_RAW_TEMPLATES` (14 тем, 427 строк)
  вынесен в `core/profile_templates.py` (`DEFAULT_RAW_TEMPLATES`, 0 зависимостей).
  `profile_render.py` импортирует под старым именем → все обращения работают. 1038→611 строк.
- ✅ **СДЕЛАНО (2026-06-23, с доступом к БД):** `database.py` — `init_db` (1048 строк, одна
  функция) разнесён на 6 доменных под-функций `_init_users_and_chats / _init_inventory_and_pets /
  _init_progression_and_economy / _init_events_and_moderation / _init_features_extra /
  _init_indexes`; `init_db` теперь оркестратор (schema/search_path + вызовы). Разнос
  AST-вырезкой по границам между top-level операторами (не внутри for/try/триплкавычек),
  де-индент 8→4. **Верификация:** capture-harness — старый и новый `init_db` с мок-БД дают
  ИДЕНТИЧНУЮ последовательность **139 SQL-вызовов** (whitespace-norm); read-only сверка с
  живым прод-Postgres — все **65 таблиц + 22 индекса** из файла реально есть в схеме
  `predvestnik`. `py_compile` ок.

## 6. ПРАВИЛА РАБОТЫ (напоминание)
- Отвечать по-русски. Сайт == бот (единый источник правды через `services/`).
- `services/` не импортирует `bot.*`/`FastAPI.*`. SQL — в репозиториях/сервисах (как battle_pass).
- app.js — classic script: `let/const` аккуратно, `${...}` только в backtick-строках, `node --check`.
- PGAdapter: `?`→`$N`; `ON CONFLICT ... table.column + $N`.
- Не трогать корень `g:\IESA_ROOT\` (Django) и `frontend/` (мёртвый React).
- Перед «не работает на проде» — сначала факты/логи (DevTools console, DO deploy logs), потом код.
