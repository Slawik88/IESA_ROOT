# BOT_AUDIT.md — Проблемные места бота (аудит 2026-07-16, доп. 2026-07-18: система реста)

> **Статус 2026-07-23: П1–П6 все ПОЧИНЕНЫ** (транзакция стрика, скидка Черепахи
> магазин+гача level/slot-aware, карточка Волка/Дракона в мёртвом bot/handlers/zoo.py,
> инверсия зависимости scheduler→FastAPI). Регресс-тесты: `tools/test_streak_atomicity.py`,
> `tools/test_turtle_discounts.py`. Ждёт деплоя.

> Запрос: «выявить проблемные места бота». Метод: не догадки по названиям, а проверка
> на живом коде — AST-скан всех `.py` (без `.venv`), сверка таблиц бонусов из
> `core/constants.py` с их реальными читателями, разбор границ `try/except`, проверка
> семантики commit в `PGAdapter`, скан всех мутирующих эндпоинтов на авторизацию.
> Каждый пункт ниже **воспроизведён и подтверждён**, а не выведен по паттерну.
>
> Структурный аудит (god-файлы, SQL вне repositories) — отдельно, в `CODE_STRUCTURE_AUDIT.md`.
> Модерация бот↔сайт — закрыта 2026-07-16, коммит `c0bbe8b7`.
>
> **Как чинить:** пиши «делаем пункт П1» (или «делаем П1, П2»).

---

## Главный вывод

Тот самый класс бага, который ты нашёл руками в модерации («бот != сайт»), **живёт и в
игровой экономике**. Плюс к нему обнаружен близкий родственник: **«код != обещание игроку»** —
когда карточка питомца показывает одно, а начисляется другое. Ниже 6 подтверждённых пунктов,
два из них — 🔴 про реальные деньги игроков.

Приоритет на мой взгляд: **П1 → П2 → П3 → П5 → П4 → П6.**

---

## ✅ П1. Стрик: награда не атомарна, потеря — молча и навсегда — ПОЧИНЕНО 2026-07-23

> Фикс: claim дня + начисление награды + жетон block-end обёрнуты в
> `async with db.connection.transaction()` (тот же паттерн, что уже у
> `transfer_currency`/`family_bank_transaction`) — сбой любого шага откатывает
> claim целиком, игрок получит награду со следующего сообщения вместо потери дня
> навсегда. Внешний `except Exception: pass` заменён на `logger.exception(...)`.
> Регресс-тест: `tools/test_streak_atomicity.py` (симулирует сбой `add_balance`
> ПОСЛЕ успешного claim, проверяет реальный rollback).



**Файл:** `bot/middlewares/streak_mw.py:74–220`

День забирается атомарно и **коммитится сразу** (`upsert_global_streak`, стр. 91), награда
начисляется **следующим отдельным запросом** (`add_balance`, стр. 103), и всё это обёрнуто
во внешний `try` (74), который заканчивается `except Exception: pass` (219–220).

Транзакции здесь нет. Проверено: `PGAdapter.commit()` (`infrastructure/pg_adapter.py:269–273`)
— *«In auto-commit mode (no active tx) this is a no-op»*, а `db.connection.transaction()` в
этом файле не используется вообще. То есть `await db.commit()` на стр. 147 — пустышка,
а каждый запрос коммитится сам по себе.

**Сценарий отказа:** любой сбой БД между стр. 91 и 103 (таймаут/обрыв — а `DB TimeoutError`
в этом проекте уже случался) → день помечен взятым, Мора и 💎 не начислены, исключение
проглочено. Игрок теряет дневную награду **навсегда** (завтра день уже «взят»), и об этом
**не остаётся ни строчки в логах** — `pass` без `logger`.

**Как правильно** — рядом в том же проекте есть эталон: `transfer_currency()` и
`family_bank_transaction()` оба заворачивают чтение+запись в `async with db.connection.transaction()`.

**Фикс:** обернуть «забрать день + начислить награду» в `db.connection.transaction()` (сбой →
откат заявки на день → игрок получит награду со следующего сообщения) и заменить `pass` на
`logger.exception(...)`. Внешний `except: pass` сейчас прячет *любые* поломки стрика, включая
логические — система может быть сломана месяцами, и никто не узнает.

---

## ✅ П2. Скидка Черепахи заморожена на 5% для всех уровней — ПОЧИНЕНО 2026-07-23

> Фикс: `apply_discount(bool)` удалён (был единственным потребителем хардкода
> Lv5). `get_item_prices()`/`purchase_item()`/каталог сайта и бота теперь берут
> `get_turtle_discount()` → реальный уровень+слот. Мигрированы ВСЕ вызывающие
> места, не только упомянутые в аудите: `FastAPI/routers/shop.py` (каталог +
> покупка), `bot/handlers/shop.py` (мёртвый код-референс, не подключён, но
> оставлен консистентным). Регресс-тест: `tools/test_turtle_discounts.py`.



**Файлы:** `services/economy.py:44–50` (корень), `:58–78`, `:95–97`

```python
def apply_discount(self, price, has_discount: bool) -> int:
    if not has_discount:
        return int(price)
    return int(price * (1.0 - TURTLE_BONUSES[5]["shop_discount"]))   # ← Lv5 ХАРДКОД
```

Реальный уровень читается (`_get_turtle_shop_discount` возвращает точную долю), но тут же
**схлопывается в `bool`** через `has_turtle_discount()` — и уровень теряется. Дальше всем
подряд применяется скидка пятого уровня.

| Уровень Черепахи | Обещано (`TURTLE_BONUSES`) | Реально в магазине | Итог |
|---|---|---|---|
| Lv1 | −2% | **−5%** | игрок переплачен |
| Lv5 | −5% | −5% | ✅ совпало |
| Lv8 | −7% | **−5%** | игрок обделён |
| Lv10 | −10% | **−5%** | игрок обделён вдвое |

**Бьёт и по боту, и по сайту** — оба пути (витрина и списание) зовут `apply_discount`.
Согласованно неправильно: расхождения бот↔сайт нет, но оба врут одинаково.

**Отдельно скверно:** карточка питомца показывает **настоящее** значение по уровню
(`bot/handlers/zoo.py:359`, `services/zoo.py:122`, `app.03.js:353` — все читают
`shop_discount` из таблицы). То есть **карточка обещает «−10% магазин», а касса даёт −5%**.
Описание вида в реестре тоже обещает: *«Lv10 — максимальные скидки»*.

Правильный API **уже написан, но мёртв**: `get_turtle_discount()` (`:40`) и
`apply_discount_fraction()` (`:52`) — **ноль вызовов** по всему проекту (проверено grep).
Докстринг сам признаётся: *«defaults to the Lv5 value to preserve behaviour; new callers
should use get_turtle_discount + multiply manually»* — временная затычка, которую забыли снять.

**Фикс:** перевести `get_item_prices()` и `purchase_item()` на `get_turtle_discount()` +
`apply_discount_fraction()`, `apply_discount(bool)` удалить.

---

## ✅ П3. Черепаха Lv8+: «−10% на крутку гачи» не реализовано вообще — ПОЧИНЕНО 2026-07-23

> Решение владельца: реализовать скидку (не убирать обещание). Фикс:
> `services/gacha.py::roll_single/roll_multi` теперь читают
> `gacha_daily_discount` через тот же `get_species_bonus()` и применяют к
> стоимости крутки (mora/diamonds) ДО списания; в мульти-крутке складывается
> мультипликативно с обычной ×10-скидкой (не суммой долей). Токены по-прежнему
> в приоритете и скидку не касаются. Регресс-тест: `tools/test_turtle_discounts.py`.



**Файлы:** `core/constants.py:156–158` (объявлено), `services/zoo.py:125–126` +
`app.03.js:355` (показано), **применено — нигде**.

Строгая проверка (grep по всем `.py` и `.js`): `gacha_daily_discount` встречается **только**
в объявлении и в двух местах отображения. Слово `turtle` **не встречается ни разу** в
`services/gacha.py`, `services/daily_deal.py`, `FastAPI/routers/gacha.py`,
`bot/handlers/gacha.py`, `bot/handlers/daily_deal.py`, `FastAPI/routers/daily_deal.py`.

Игроку обещано в реестре (`core/registry.py:185`): *«Lv8 — скидка на крутку гачи»*, в
карточке рисуется «🏷 −10% акция дня» — **и не даётся никогда**.

Это ровно тот же класс, что уже дважды чинили: декоративный `bank_bonus` Дракона (БЛОК 36.3)
и `movement_immunity` Волка. Я прогнал скан всех ключей бонусов всех питомцев — **полностью
мёртвых ключей больше не осталось**, этот последний: он «читается», но только файлами
отображения.

**Фикс (нужно твоё решение):** либо реально применить скидку к стоимости крутки, либо
убрать обещание из таблицы и карточки. Первое — честнее по отношению к тем, кто качал
Черепаху до Lv8 ради этого.

---

## ✅ П4. Скидка Черепахи игнорирует слот (нарушение «Блока 12») — ПОЧИНЕНО 2026-07-23 (вместе с П2)

> Фикс: `_get_turtle_shop_discount` (ручной SQL без ORDER BY) заменён на
> `get_species_bonus(db, user_id, 'turtle')` — тот же аксессор, что уже
> корректно учитывает слот (`scale_pet_bonus`, passive ×0.5) и порядок
> (`ORDER BY (placement='active') DESC`) для экспедиций/квестов/зоопарка.
> Второй фикс (скидка на гачу, П3) идёт по этой же цепочке.



**Файл:** `services/economy.py:12–24`

```python
"SELECT COALESCE(pet_level, 1) FROM pets "
"WHERE owner_id = ? AND placement IN ('active','passive') "
"AND species_id = 'turtle' AND fatigue < 100 LIMIT 1"
```

Два дефекта:

1. **Нет `scale_pet_bonus()`.** По «Блоку 12» питомец в пассивном слоте даёт **половину**
   бонуса. Магазин — **единственный** потребитель бонусов питомцев, который ходит в БД сам
   вместо готового `get_species_bonus()` (`infrastructure/repositories/zoo.py:176`), который
   слот учитывает. Экспедиции, квесты, зоопарк — все используют правильный аксессор.
   А у Черепахи `default_role` = **`"passive"`** — то есть почти у всех она именно в том
   слоте, где скидку положено делить пополам.
2. **`LIMIT 1` без `ORDER BY`.** Если у игрока две Черепахи (актив + пассив), выбирается
   произвольная. Эталон рядом — `get_species_level_placement()` делает
   `ORDER BY (placement = 'active') DESC`.

Сейчас это замаскировано пунктом П2 (значение всё равно выбрасывается в bool), поэтому
**чинить нужно вместе с П2** — иначе фикс П2 включит полную скидку и пассивным Черепахам.

---

## ✅ П5. Карточка питомца в боте показывает нули — мёртвые имена ключей — ПОЧИНЕНО 2026-07-23

> Важная находка при фиксе: `bot/handlers/zoo.py` — **DEPRECATED, не подключён**
> к `main_router` (см. заголовок файла, БЛОК19 «Web First») — эти неверные ключи
> реально НЕ показывались ни одному живому игроку, только в оставленном как
> референс мёртвом коде. Всё равно исправлено на настоящие ключи
> (`passive_reduction`/`active_reduction`/`food_extra`/`daily_restore_uses`+
> `daily_restore_amount`/`movement_immunity` для Волка,
> `bank_bonus`/`free_food_chance`/`hamster_collect_bonus`/`weekly_bank_grant`
> для Дракона) — чтобы если файл когда-то реанимируют, он не соврал снова.



**Файл:** `bot/handlers/zoo.py:362–367`

Бот читает ключи, **которых не существует** в таблицах бонусов (проверено на живых данных):

| Строка | Читает | Есть в таблице? | Что видит игрок |
|---|---|---|---|
| `:363` | `nursery_fatigue_reduce` (Волк) | ❌ нет | «🐾 Питомник: **−0%** усталости от перемещений» |
| `:364` | `daily_fatigue_restore` (Волк) | ❌ нет | строка восстановления **не показывается никогда** |
| `:367` | `family_bank_cap_bonus` (Дракон) | ❌ нет | «🏦 Семейный банк **+0** 🪙 кап» |

Настоящие ключи: у Волка — `passive_reduction` / `active_reduction` / `movement_immunity` /
`daily_restore_amount` / `daily_restore_uses` / `food_extra`; у Дракона — `bank_bonus` /
`free_food_chance` / `hamster_collect_bonus` / `weekly_bank_grant`.

Проверено: эти три имени **не встречаются больше нигде** в проекте — чистые опечатки-призраки,
пережившие переименование таблиц.

**И снова бот != сайт:** сайт (`services/zoo.py:139–169`) и мини-апп (`app.03.js:360–363`)
используют **правильные** ключи и показывают всё корректно. Владелец Волка Lv10 на сайте
видит «иммунитет к перемещениям», а в боте — «−0% усталости».

**Фикс:** переписать ветки `wolf`/`dragon` на настоящие ключи, взяв за образец
`services/zoo.py::pet_bonus_line` (а лучше — звать его же, чтобы третьего расхождения не завелось).

---

## ✅ П7. Щит новичка при возврате в чат стирает постоянный иммунитет — ПОЧИНЕНО 2026-07-18

> Фикс: `bot/handlers/events.py` теперь зовёт `mod_service.shield_user()` (сохраняет
> `is_immune`, пишет журнал, сам создаёт строки users/user_chat_stats).

**Файл:** `bot/handlers/events.py:267–280`

Автовыдача «щита новичка» при входе в чат (`shield_duration_days` > 0) пишет иммунитет
напрямую, в обход единого сервиса:

```python
await mod_db.set_immunity(db, chat_id, user_id, 0, until_str)   # ← is_immune ЖЁСТКО 0
```

Все остальные три точки записи (`shield_user`, `unshield_user`, `set_immune_user` в
`services/moderation.py`) сначала читают текущий `is_immune` и передают его дальше
(`keep_immune`), чтобы временный щит не сбрасывал постоянный. Этот путь — единственный,
который этого не делает.

**Сценарий отказа:** игроку выдан постоянный иммунитет (`бот иммунитет`). Он выходит из
чата и возвращается (или его кикнули/разбанили) в чате, где включён «щит новичка»
(`бот настройка щита`). Иммунитет молча превращается в обычный временный щит на
`shield_duration_days` дней — после его истечения защита пропадает насовсем, и никто
не увидит это в логах (`set_immunity` не пишет `moderation_logs`, в отличие от
`shield_user`/`set_immune_user`, которые зовут `log_moderation_action`).

Это ровно тот класс бага, что уже чинили в `cbb09db8` («защита/рест: критический баг с
перезаписью иммунитета») — но фикс применили только к ручной команде `бот рест`, этот
параллельный путь (авто-щит при входе) остался нетронутым.

**Фикс:** заменить прямой вызов на `mod_service.shield_user(db, chat_id, user_id,
bot.id, shield_days*1440)` (он сам сохранит `is_immune` и напишет журнал), либо руками
прочитать `get_chat_stats` и передать `keep_immune` — как везде.

---

## 🚫 П8. Тумблер «бот иммунитет» стирает активный временный рест — WONTFIX (решение 2026-07-18)

> Пользователь решил: не трогать. Иммунитет-тумблер всегда чистит всё — это принятое
> поведение продукта, не баг. Не поднимать повторно.

**Файл:** `services/moderation.py:259–264`

```python
async def set_immune_user(db, chat_id, target_id, admin_id, on, reason=None):
    """Постоянный иммунитет вкл/выкл. Включение/снятие сбрасывает и временный щит."""
    await set_immunity(db, chat_id, target_id, 1 if on else 0, None)
```

`immune_until` жёстко обнуляется при ЛЮБОМ переключении (и включении, и выключении),
независимо от того, активен ли сейчас временный рест. Комментарий в коде выдаёт это
за намеренное поведение, но по факту это побочный эффект, о котором никого не
предупреждают.

**Сценарий отказа:** админ A выдал `бот рест, @юзер 7д`. На 2-й день админ B (по другой
причине — тестирует права, ошибся командой) включает и тут же выключает `бот иммунитет`
для того же юзера. Оставшиеся 5 дней реста исчезают без единого сообщения об этом —
ни экрана подтверждения, ни строчки в `бот кто рест`.

**Фикс:** если `immune_until` в будущем и `on == False` (снимаем постоянный, но был
активный рест) — сохранить `immune_until` как есть вместо `None`. Если это осознанное
решение продукта (иммунитет — только «всё или ничего»), тогда хотя бы предупреждать
админа в ответе бота: «⚠️ у юзера был активный рест до ДАТА — он тоже снят».

---

## ✅ П9. Ранговая асимметрия: рест выдать легче, чем снять — ПОЧИНЕНО 2026-07-18

> Фикс: `cmd_unprotect` перешёл на `check_admin_rights` (симметрично выдаче,
> без сравнения с рангом цели).

**Файлы:** `bot/handlers/moderation.py:357–362` (выдача) vs `:401–406` (снятие)

Выдача реста проверяет `check_admin_rights` — только ранг инициатора ≥ `rank_shield`,
**без сравнения с рангом цели**. Снятие (и `снять рест`, и постоянный `иммунитет`) идёт
через `_check_target_rights` → при цели ≠ себе вызывает `check_mod_rights`, который
**требует `target_rank < admin_rank`**.

**Сценарий отказа:** админ с рангом 4 (`rank_shield` по умолчанию) защищает Владельца
чата (ранг 6) — команда проходит, target-проверки нет. Через неделю снять этот же рест
он уже не может: `check_mod_rights` вернёт «⚠️ Вы не можете наказать того, чей ранг
равен вашему или выше» — хотя речь не о наказании, а о снятии защиты. Рест «залипает»
до ручного вмешательства более высокого ранга (или самого Владельца).

**Фикс:** либо выдачу реста тоже гонять через target-сравнение (нельзя защитить того,
кто выше тебя), либо снятие — через `check_admin_rights` без сравнения (симметрично
выдаче). Сейчас несимметрично в обе стороны нежелательным образом.

---

## ✅ П10. `parse_time` без верхней границы — краш на огромном числе — ПОЧИНЕНО 2026-07-18

> Фикс: `parse_time` бросает `TimeTooBigError` при сроке > 3650д; рест/варн/мут отвечают
> явным «Слишком большой срок. Максимум 3650д»; глоб-санкции оставляют число в причине.

**Файл:** `bot/handlers/moderation.py:43–57`, используется в `cmd_warn`, `cmd_protect`

```python
match = re.match(r"^(\d+)([смчдsmhd])$", time_str.lower())
...
return timedelta(days=val)
```

`\d+` не ограничен по длине. `timedelta` в Python хранит максимум ~999 999 999 дней —
за пределами этого `timedelta(days=val)` кидает необработанный `OverflowError` прямо
внутри `parse_time`, до всех `try/except`.

**Сценарий отказа:** `бот рест, @юзер 99999999999999д` (опечатка лишний ноль, не злой
умысел — ранг для этой команды и так ограничивает круг лиц) → хендлер падает
исключением вместо ожидаемого «❌ Неверный формат времени!», админ получает тишину.

**Фикс:** обернуть `timedelta(...)` в `try/except (OverflowError, ValueError): return None`
внутри `parse_time` — один раз чинит и `варн`, и `рест`.

---

## ✅ П6/П11. `services/scheduler.py` импортирует `FastAPI.*` — ПОЧИНЕНО 2026-07-23

> Фикс (инверсия зависимости): `services/scheduler.py` теперь хранит
> регистрируемый колбэк `_ws_notify_cb` (`set_ws_notify()`), без импорта
> `FastAPI.*`. `bot/__main__.py` (уже импортирует `FastAPI.main` при старте —
> ему это можно) регистрирует `FastAPI.notifications.notify` в это место сразу
> после старта uvicorn. Поведение не изменилось (тот же fallback на
> `web_notifications`, если WS недоступен) — изменился только *откуда*
> импортируется транспорт. Проверено: `grep` по `services/` на `FastAPI` —
> ноль совпадений.

**Было:** `services/scheduler.py:219` — `from FastAPI.notifications import notify as _ws_notify`,
единственное нарушение железного правила `BASE_PROMPT.md` («services/ не импортирует
bot.\*/FastAPI.\*») по всему `services/`. Импорт был ленивый и обёрнут в `try/except`
с откатом на запись в БД — не падало, но было дырой в слоях архитектуры.

---

## 🟢 Проверено — здесь чисто (чтобы не перепроверять)

- **Авторизация API — дыр нет.** AST-скан: **161 из 161** мутирующих эндпоинтов
  (POST/PUT/PATCH/DELETE) во всех роутерах, включая dev-консоль, имеют проверку личности
  (`require_tg_user` / `require_developer` / `require_console_perm` / `_require_admin`).
- **Переводы валют и семейный банк — эталон.** `transfer_currency()` и
  `family_bank_transaction()`: `FOR UPDATE` + `db.connection.transaction()` + `wallet_log`.
  Защита от двойной траты и гонки супругов на месте. Кап общака от Дракона реально проверяется.
- **Гача — единая на бот и сайт.** Pity (soft/hard), таблицы, ставки — всё в
  `services/gacha.roll_single/roll_multi`, обе стороны зовут один код. Расхождений нет.
- **`expedition_discount` Черепахи — реализован правильно** (учитывает уровень и слот с обеих
  сторон). Именно поэтому П2/П3 выглядят особенно обидно: соседняя скидка того же питомца
  сделана как надо.
- **146 `except: pass` по проекту** (51 — вокруг операций состояния) — разобраны. Подавляющее
  большинство намеренны и защищают главное действие от падения ачивок/квестов
  (`increment_metric` и т.п.) — это нормальный, осознанный паттерн. Единственное опасное
  место — П1.
- **Полностью декоративных бонусов питомцев больше нет** — скан всех ключей всех таблиц;
  единственный оставшийся «показывается, но не применяется» — П3.

---

## Сводка

| # | Проблема | Кого бьёт | Класс |
|---|---|---|---|
| ✅ П1 | Стрик: награда не атомарна, потеря молча и навсегда | починено 2026-07-23 | надёжность/деньги |
| ✅ П2 | Скидка Черепахи заморожена на 5% | починено 2026-07-23 | деньги/обещание |
| ✅ П3 | «−10% на крутку гачи» не реализовано | починено 2026-07-23 | обещание |
| ✅ П4 | Скидка Черепахи игнорирует слот и берёт случайную | починено 2026-07-23 (вместе с П2) | «Блок 12» |
| ✅ П5 | Карточка Волка/Дракона в боте показывает нули | починено 2026-07-23 (был мёртвый код) | бот != сайт |
| ✅ П6 | `services/` импортирует `FastAPI.*` | починено 2026-07-23 | архитектура |
| ✅ П7 | Щит новичка стирал постоянный иммунитет | починено 2026-07-18 | защита |
| 🚫 П8 | Тумблер «иммунитет» стирает активный рест | WONTFIX — решение продукта | защита |
| ✅ П9 | Рест: дать легче, чем снять | починено 2026-07-18 | UX/права |
| ✅ П10 | Краш parse_time на огромном числе | починено 2026-07-18 | надёжность |

*Аудит: 2026-07-16, доп. 2026-07-18 (система реста/иммунитета). Все пункты подтверждены на коде, не по догадке.*
