# Аудит багов Предвестник — 2026-06-09

Блоки упорядочены по зависимостям: фикс блока N не ломает блок N+1.
Блоки 1–2 фундаментальные — без них тестировать остальное ненадёжно.

---

## ~~Блок 1 — Временная зона~~ ✅ ГОТОВО
| # | Баг | Фикс |
|---|-----|------|
| 1 | Время на сайте расходится с ботом на 2 часа | `app.js`: добавлен `fmtUTC()`, заменены все `.slice(0,16)` для `created_at` |

---

## ~~Блок 2 — Критические баги экономики~~ ✅ ГОТОВО
| # | Баг | Фикс |
|---|-----|------|
| 2 | "Недостаточно средств" при покупке x5/x10 | `services/economy.py`: `purchase_item` передавал суммарную стоимость в `buy_item`, а та умножала ещё раз на qty → цена ×qty² вместо ×qty |
| 4 | Награда за чек-ин выдаётся дважды | `streak.py`: `upsert_streak` теперь возвращает `bool` через `RETURNING` + `WHERE last_notified::date <`; `streak_mw.py`: клейм вызывается ДО выдачи награды |

---

## ~~Блок 3 — Система питомцев~~ ✅ ГОТОВО

| # | Баг | Фикс |
|---|-----|------|
| 7  | При смене активного питомца можно отправить другого питомца параллельно | `expeditions.py`: проверка по `owner_id` через JOIN вместо `WHERE pet_id` |
| 8  | Награда за уровень не видна в истории операций (отображалась как "system") | `services/zoo.py`: `source="pet_milestone"` в `add_balance`; `wallet.py`: лейбл добавлен |
| 16 | Хомяк не накапливает монеты (pending = 0 навсегда) | `zoo.py repo`: фоллбэк на 24ч назад при NULL `last_income_collection` + инициализация |

---

## ~~Блок 4 — Гача и пити~~ ✅ ГОТОВО

| # | Баг | Фикс |
|---|-----|------|
| 6 | Счётчик пити инкрементируется рандомно | `gacha.py repo`: `get_pity_locked` (INSERT DO NOTHING + SELECT FOR UPDATE); `incr_pity` использует RETURNING; `services/gacha.py`: `roll_single/multi` используют `get_pity_locked` + локальный трекинг пити в multi |
| 5 | История гачи в боте на вечной загрузке | `bot/handlers/gacha.py`: `query.answer()` перенесён до DB-вызова — Telegram получает ответ до того, как может истечь 10-секундный таймаут |

---

## ~~Блок 5 — Дуэли~~ ✅ ГОТОВО

| # | Баг | Фикс |
|---|-----|------|
| 11 | Статус дуэли зависает в "processing" после нехватки моры у оппонента | `services/duel.py`: в `accept_duel` добавлен cleanup — `set_duel_status('declined')` + `_remove_reserve(challenger)` внутри блока транзакции перед `return False` |

---

## ~~Блок 6 — Достижения и стрик~~ ✅ ГОТОВО

| # | Баг | Фикс |
|---|-----|------|
| 13 | Ачивки считают стрик неправильно (стрик = 8, ачивка показывает 4) | `streak_mw.py`: `max_streak_ever` теперь читает `MAX(streak)` по всем чатам пользователя вместо per-chat `result["new_streak"]`; `handlers/achievements.py`: добавлен sync "persistent" через `MAX(streak)` при просмотре ачивок (аналогично "talker") |

---

## ~~Блок 7 — Настройки бота / системные флаги~~ ✅ ГОТОВО

| # | Баг | Фикс |
|---|-----|------|
| 9  | Защита "бот защита @ [Nд]" не выдаётся | `services/moderation.py`: `check_admin_rights` не принимала `bot_id` → `TypeError` на каждом вызове из `cmd_protect` (и cmd_immunity, cmd_unprotect) → добавлен параметр `bot_id: int = 0` |
| 15 | Бот кидает сундуки в чат, даже если функция отключена в настройках | `infrastructure/repositories/chest_events.py`: `get_qualifying_chats` не проверяла `events_enabled` → добавлено `AND COALESCE(cs.events_enabled, TRUE) = TRUE` |

---

## ~~Блок 8 — UI / Фронтенд~~ ✅ ГОТОВО

| # | Баг | Фикс |
|---|-----|------|
| 3  | Toast/ошибка появляется под модальным окном (z-index) | `app.js`: `toast()` перемещает `#toast` внутрь `<dialog>` когда он открыт (top-layer); `CM()` возвращает обратно в `<body>` |
| 10 | Купоны не обновляются без перезагрузки страницы | `app.js`: `loadShopCatalog()` переписан на `Promise.all([inv, shop])` — инвентарь всегда загружается заново перед рендером каталога |
| 12 | Кнопка "Купи 🏡 Расширитель в Магазине" не открывает вкладку Магазин | `app.js`: оба вызова `CM();swMkt('shop',...)` заменены на `goTo('market','shop')` — правильная навигация через существующую функцию |
| 17 | Смена ника на сайте не отражается в "бот я" | `app.js`: `loadNickCard()` и `saveNick()` используют `_initChatId\|\|_cid` — чат из WebApp-контекста вместо первого чата из API |

---

## Блок 9 — Баланс / Фича `★☆☆`
> Не баг, а изменение баланса. Делать последним, чтобы не мешать тестированию.

| # | Задача |
|---|--------|
| 14 | Убрать КД с игр и повысить шанс выигрыша |

---

## Сводная таблица

| Блок | Тема | Багов | Сложность |
|------|------|------:|-----------|
| 1 | Временная зона | 1 | ★☆☆ |
| 2 | Критическая экономика | 2 | ★★☆ |
| 3 | Питомцы | 3 | ★★☆ |
| 4 | Гача / пити | 2 | ★★★ |
| 5 | Дуэли | 1 | ★★☆ |
| 6 | Достижения | 1 | ★★☆ |
| 7 | Настройки бота | 2 | ★★☆ |
| 8 | UI / Фронтенд | 4 | ★☆☆–★★☆ |
| 9 | Баланс (фича) | 1 | ★☆☆ |
| **Итого** | | **17** | |
